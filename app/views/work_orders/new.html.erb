<!--mytodo - heaps to fix up here....add tabs and all fields required.-->
<div id="top_float_right">
  <%= link_to(image_tag("close.png", :title=> (t "global.exit"), :align => "bottom", :hspace => "2"), work_orders_path)%><%= link_to (t "global.exit"), work_orders_path %>
  <%= link_to(image_tag("back.png", :title=> (t "global.back"), :align => "bottom", :hspace => "2"), :back)%><%= link_to (t "global.back"), :back %>
</div>
<div id="ddtabs1" class="basictab">
<ul>
<li><a href="#<%= t "workorder.tab_menu_basic_name"  %>" rel="sc1"><%= (t "workorder.tab_menu_basic") %></a></li>
</ul>
</div>
<div class="tabcontainer">
<div id="sc1" class="tabcontent">
<% semantic_form_for(@work_order) do |f| %>
      <% f.inputs do %>
        <%= f.input :user_id, :as=> :hidden, :value => params[:client_id] %>
        <%= f.input :subject, :label => (t "workorder.subject") %>
        <%= f.input :priority_list, :label => (t "workorder.priority"), :include_blank => false %>
        <%= f.input :assigned_to_username, :as => :select, :collection => User.find(:all, :conditions => "roles_mask < 5 " )  %>
        <%= f.input :description,:label => (t "global.description"), :input_html => {:rows => 25, :style => "margin-left:26%;"}%>
        <%= f.input :edited_by, :as => :hidden, :input_html => {:value =>  current_user.username} %>
        <%= f.input :created_by, :as => :hidden, :input_html => {:value =>  current_user.username} %>
        <%= f.input :updated_at, :as => :hidden, :input_html => {:value =>  Time.now} %>

      <% end %>
<%= f.buttons %>
 <%= textiletoolbar_for('work_order_description') %>
<% end %>
</div>
</div>
