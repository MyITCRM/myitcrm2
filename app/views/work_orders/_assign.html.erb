 <% form_for @work_order, :url =>{:action => "assign"} do |f| %>
     <%= f.collection_select(:assigned_to, User.find(:all, :conditions => "roles_mask < 5 " ),:id, :name)%>
     <%= hidden_field_tag(:id, @work_order.id ) %>
     <%= button_to "Assign" %>
<% end %>