<h1>Editing work_order</h1>

<% form_for(@work_order) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :workorder_id %><br />
    <%= f.text_field :workorder_id %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @work_order %> |
<%= link_to 'Back', work_orders_path %>